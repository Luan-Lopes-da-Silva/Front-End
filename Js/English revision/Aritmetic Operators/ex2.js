export default function(){
  const num = parseFloat(prompt('Type an number'))
  alert(`
  The some of ${num} + ${num} is = ${num*2}
  The number ${num} to the square is equal to = ${num**2}
  The number ${num} to the cube is equal to = ${num**3}
  `)
}